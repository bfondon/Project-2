

    <div class="dashboard">
        <div class="heading">
            <img src="" alt="">
            <p>{{userFullName}},welcome to 10000 challenge</p>
        </div>
{{!-- <nav class="navbar navbar-expand-lg navbar-light bg-light">
 <a class="navbar-brand" href="#">Navbar</a>
 <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
   <span class="navbar-toggler-icon"></span>
 </button>
 <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
   <div class="navbar-nav">
     <a class="nav-item nav-link active" href="#">Home <span class="sr-only">(current)</span></a>
     <a class="nav-item nav-link" href="#">Features</a>
     <a class="nav-item nav-link" href="#">Pricing</a>
     <a class="nav-item nav-link disabled" href="#">Disabled</a>
   </div>
 </div>
</nav> --}}
        <div class="container">
            <div class ="col-8 offet-2">
                <div class="add-button">
                <button id="addButton" >+</button>
                <span>add habit</span>
                <input type="text" id="habitname">
                Goal <input type="text" id="goal">
                Achieved <input type="text" id="achieved">
                </div>
                <br>
                <div class="habits">

                      {{#each allHabits}}
                        <p>Habit: <div id="habit{{id}}">{{habitname}}</p>
                            <p>Goal:{{goal}} </p>
                            <p>Achieved so far:{{achieved}}</p>
                        </div>
                    {{/each}}
                </div>
            </div>
            
        </div>
    </div>
 <script>
        $("#addButton").click(function (e) {
            e.preventDefault();

            var newHabit = {
                habitname: $("#habitname").val().trim(),
                goal: $("#goal").val().trim(),
                achieved: $("#achieved").val().trim()
            }
            
            console.log(newHabit);

            $.ajax("/api/habits", {
                type: "POST",
                data: newHabit
            }).then(function (res) {
                console.log("habit added.")
                // var div = $("<div>");
                // div.attr("id", "res.???");
                // div.text(res.habitname);
                // div.append("<p>" + res.goal  + "</p>");
                // div.append("<p>" + res.achieved + "</p>");
                // $(".habits").append(div);
                // console.log("created new habit");
                // location.reload();
            })


        });

    </script>
